name: Monitoring CronJobs Docker Image CI

on:
  push:
    tags:
      - 'monitoring-*'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        
      - name: Get tag name
        id: tagname
        run: echo "tag=${GITHUB_REF#refs/tags/monitoring-}" >> "$GITHUB_ENV"
        
      - name: Login to CERN Harbour
        uses: docker/login-action@v2
        with:
          registry: registry.cern.ch
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_TOKEN }}

      - name: Build the Docker Image
        run: |
          cd docker/monitoring && docker build . \
          --file Dockerfile \
          --tag registry.cern.ch/${{ vars.HARBOR_REPOSITORY }}/dmops-spark-crons:${{ env.tag }}
      
      - name: Push Image to CERN Harbour
        run: docker push registry.cern.ch/${{ vars.HARBOR_REPOSITORY }}/dmops-spark-crons:${{ env.tag }}

