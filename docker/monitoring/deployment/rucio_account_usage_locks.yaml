apiVersion: batch/v1
kind: CronJob
metadata:
  name: rucio-account-usage-locks
  namespace: dmtops-crons
spec:
  schedule: "30 6 * * *" 
  jobTemplate:   
    spec:
      template:
        metadata:
          labels:
            app: dmtops-spark-svc-1
        spec:
          containers:
          - name: rucio-account-usage-locks
            image: registry.cern.ch/cmsrucio/dmops-spark-crons:latest
            imagePullPolicy: Always
            env:
            - name: DRIVERPORT
              value: "31205"
            - name: BMPORT
              value: "31206"
            - name: K8SHOST
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            command: ["/src/submit_rucio_account_usage_locks.sh"]
            ports:
            - containerPort: 31205 # spark.driver.port
              name: port-0
            - containerPort: 31206 # spark.driver.blockManager.port
              name: port-1
            volumeMounts:
              - name: dmtops-cron-secrets
                mountPath: /etc/secrets
                readOnly: true
          volumes:
          - name: dmtops-cron-secrets
            secret:
              secretName: dmtops-cron-secrets
          restartPolicy: OnFailure