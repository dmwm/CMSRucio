apiVersion: batch/v1
kind: CronJob
metadata:
  name: rucio-activity-usage
  namespace: dmtops-crons
spec:
  schedule: "13 3 * * *"
  jobTemplate:
    spec:
      template:
        metadata:
          labels:
            app: dmtops-spark-svc-3
        spec:
          containers:
          - name: rucio-activity-usage
            image: registry.cern.ch/cmsrucio/dmops-spark-crons:latest
            imagePullPolicy: Always
            env:
            - name: DRIVERPORT
              value: "31209"
            - name: BMPORT
              value: "31210"
            - name: K8SHOST
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            command: ["/src/submit_rucio_activity_usage.sh"]
            ports:
            - containerPort: 31209 # spark.driver.port
              name: port-0
            - containerPort: 31210 # spark.driver.blockManager.port
              name: port-1
            volumeMounts:
              - name: dmtops-cron-secrets
                mountPath: /etc/secrets
                readOnly: true
          volumes:
          - name: dmtops-cron-secrets
            secret:
              secretName: dmtops-cron-secrets
          restartPolicy: OnFailure
